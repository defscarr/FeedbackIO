
const metadata = {
    title:'dashboard',
    description:'Feedback dashboard'
}

export default function RootLayout({children}:{children: React.ReactNode}){
    return (
        <>
                {children}

        </>
    )
}